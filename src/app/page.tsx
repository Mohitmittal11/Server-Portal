import LoginSelection from "@/components/loginSelection";
import React, { Fragment } from "react";

const page = () => {
  return (
    <Fragment>
      <LoginSelection />
    </Fragment>
  );
};

export default page;
